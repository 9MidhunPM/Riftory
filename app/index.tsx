
// This file exists to satisfy expo-router's file-based routing
// The actual navigation is handled by the BottomTabNavigator in _layout.tsx
export default function Index() {
  // The main app content is rendered by the layout
  // This component won't actually render as the layout takes over
  return null;
}
